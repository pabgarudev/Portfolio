---
const PROJECTS = [
    {
        title: 'Project 1',
        description: '...',
        link: '#',
        github: '#',
        image: '#',
        tags: ['HTML', 'CSS', 'JavaScript'],
    }
]
---

{
    PROJECTS.map(({image, title, description, tags}) => (
        <article>
            <h3>{title}</h3>
            <p>{description}</p>
            <ul>
                {tags.map(tag => (
                    <li>{tag}</li>
                ))}
            </ul>
            <img src={image} alt={'Screenshot'}>
        </article>
    ))
}