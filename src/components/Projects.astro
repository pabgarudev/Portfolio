<!-- ---
const PROJECTS = [
    {
        title: 'Project 1',
        description: '...',
        link: '#',
        github: '#',
        image: '#',
        tags: ['HTML', 'CSS', 'JavaScript'],
    }
]
---

{
    PROJECTS.map(({image, title, description, tags}) => (
        <article>
            <h3>{title}</h3>
            <p>{description}</p>
            <ul>
                {tags.map(tag => (
                    <li>{tag}</li>
                ))}
            </ul>
            <img src={image} alt={'Screenshot'}>
        </article>
    ))
} -->

<div class="rounded-3xl border border-white/10 bg-white/[0.03] backdrop-blur-sm p-8 sm:p-10 text-center shadow-2xl">
  <div class="mx-auto mb-6 flex h-14 w-14 items-center justify-center rounded-2xl border border-white/10 bg-white/[0.05] text-2xl">
    🚧
  </div>

  <h2 class="text-2xl sm:text-xl font-bold text-white">
    Work in progress
  </h2>

  <p class="mx-auto mt-4 max-w-[520px] text-white/60 leading-relaxed">
    I’m currently polishing this section with selected projects, technical details,
    screenshots and links. Something cool is coming soon.
  </p>

  <div class="mt-8 flex justify-center gap-2">
    <span class="h-2 w-2 animate-pulse rounded-full bg-white/40"></span>
    <span class="h-2 w-2 animate-pulse rounded-full bg-white/40 [animation-delay:150ms]"></span>
    <span class="h-2 w-2 animate-pulse rounded-full bg-white/40 [animation-delay:300ms]"></span>
  </div>
</div>